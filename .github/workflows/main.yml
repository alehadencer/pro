name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

     jobs:
      build-and-push:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
    
        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v1
    
        - name: Log in to DockerHub
          uses: docker/login-action@v1
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
    
        - name: Set up Node.js
          uses: actions/setup-node@v2
          with:
            node-version: '20.11.1' # Обновлено до использования версии Node.js 20.11.1
    
        - name: Install dependencies
          run: npm install
    
        - name: Build and push Docker image
          uses: docker/build-push-action@v2
          with:
            context: .
            file: ./Dockerfile
            push: true
            tags: dev2228/my-react-app:latest
    
        - name: Run tests
          run: npm test
    

    # Для развертывания в Kubernetes, добавьте шаги для установки kubectl и применения конфигураций
    # Этот шаг предполагает, что у вас уже настроен Kubernetes кластер и доступы к нему
    # - name: Set up Kubeconfig
    #   uses: azure/setup-kubectl@v1
    #   with:
    #     version: 'latest'
    # - name: Deploy to Kubernetes
    #   run: |
    #     kubectl apply -f k8s/
